import {
  CollapsibleMixin,
  SummaryController,
  details,
  detailsSummary
} from "./chunk-CEMK6XXL.js";
import {
  DelegateStateMixin
} from "./chunk-7DNYTQEN.js";
import {
  DelegateFocusMixin
} from "./chunk-524SFP4O.js";
import {
  ActiveMixin
} from "./chunk-CHBKIVS3.js";
import {
  TooltipController
} from "./chunk-A54SLGAX.js";
import {
  ControllerMixin
} from "./chunk-KUB5XK2W.js";
import {
  DirMixin
} from "./chunk-ZXX3J6CF.js";
import {
  ThemableMixin,
  registerStyles
} from "./chunk-RMQPKG57.js";
import {
  defineCustomElement
} from "./chunk-HLV3SA5Y.js";
import {
  css
} from "./chunk-JOMJT2WW.js";
import {
  PolymerElement
} from "./chunk-3TSYJSTE.js";
import {
  html
} from "./chunk-XB4RSW42.js";

// node_modules/@vaadin/accordion/theme/lumo/vaadin-accordion-heading-styles.js
var accordionHeading = css`
  :host {
    padding: 0;
  }

  [part='content'] {
    padding: var(--lumo-space-s) 0;
  }

  :host([theme~='filled']) {
    padding-top: 0;
    padding-bottom: 0;
  }
`;
registerStyles("vaadin-accordion-heading", [detailsSummary, accordionHeading], { moduleId: "lumo-accordion-heading" });

// node_modules/@vaadin/accordion/src/vaadin-accordion-heading-core-styles.js
var accordionHeading2 = css`
  :host {
    display: block;
    outline: none;
    -webkit-user-select: none;
    user-select: none;
  }

  :host([hidden]) {
    display: none !important;
  }

  button {
    display: flex;
    align-items: center;
    justify-content: inherit;
    width: 100%;
    margin: 0;
    padding: 0;
    background-color: initial;
    color: inherit;
    border: initial;
    outline: none;
    font: inherit;
    text-align: inherit;
  }
`;

// node_modules/@vaadin/accordion/src/vaadin-accordion-heading.js
registerStyles("vaadin-accordion-heading", accordionHeading2, { moduleId: "vaadin-accordion-heading-styles" });
var AccordionHeading = class extends ActiveMixin(DirMixin(ThemableMixin(PolymerElement))) {
  static get is() {
    return "vaadin-accordion-heading";
  }
  static get template() {
    return html`
      <button id="button" part="content" disabled$="[[disabled]]" aria-expanded$="[[__updateAriaExpanded(opened)]]">
        <span part="toggle" aria-hidden="true"></span>
        <slot></slot>
      </button>
    `;
  }
  static get properties() {
    return {
      /**
       * When true, the element is opened.
       */
      opened: {
        type: Boolean,
        reflectToAttribute: true,
        value: false
      }
    };
  }
  /**
   * @param {DocumentFragment} dom
   * @return {null}
   * @protected
   * @override
   */
  _attachDom(dom) {
    const root = this.attachShadow({ mode: "open", delegatesFocus: true });
    root.appendChild(dom);
    return root;
  }
  /** @protected */
  ready() {
    super.ready();
    if (!this.hasAttribute("role")) {
      this.setAttribute("role", "heading");
    }
  }
  /** @private */
  __updateAriaExpanded(opened) {
    return opened ? "true" : "false";
  }
};
defineCustomElement(AccordionHeading);

// node_modules/@vaadin/accordion/theme/lumo/vaadin-accordion-panel-styles.js
var accordionPanel = css`
  :host {
    margin: 0;
    border-bottom: solid 1px var(--lumo-contrast-10pct);
  }

  :host(:last-child) {
    border-bottom: none;
  }

  :host([theme~='filled']) {
    border-bottom: none;
  }

  :host([theme~='filled']:not(:last-child)) {
    margin-bottom: 2px;
  }
`;
registerStyles("vaadin-accordion-panel", [details, accordionPanel], { moduleId: "lumo-accordion-panel" });

// node_modules/@vaadin/accordion/src/vaadin-accordion-panel-core-styles.js
var accordionPanel2 = css`
  :host {
    display: block;
  }

  :host([hidden]) {
    display: none !important;
  }

  [part='content'] {
    display: none;
    overflow: hidden;
  }

  :host([opened]) [part='content'] {
    display: block;
    overflow: visible;
  }
`;

// node_modules/@vaadin/accordion/src/vaadin-accordion-panel-mixin.js
var AccordionPanelMixin = (superClass) => class AccordionPanelMixinClass extends CollapsibleMixin(DelegateFocusMixin(DelegateStateMixin(superClass))) {
  static get properties() {
    return {
      /**
       * A text that is displayed in the heading, if no
       * element is assigned to the `summary` slot.
       */
      summary: {
        type: String,
        observer: "_summaryChanged"
      }
    };
  }
  static get observers() {
    return ["__updateAriaAttributes(focusElement, _contentElements)"];
  }
  static get delegateProps() {
    return ["disabled", "opened", "_theme"];
  }
  constructor() {
    super();
    this._summaryController = new SummaryController(this, "vaadin-accordion-heading");
    this._summaryController.addEventListener("slot-content-changed", (event) => {
      const { node } = event.target;
      this._setFocusElement(node);
      this.stateTarget = node;
      this._tooltipController.setTarget(node);
    });
    this._tooltipController = new TooltipController(this);
    this._tooltipController.setPosition("bottom-start");
  }
  /**
   * @protected
   * @override
   */
  __forwardTabIndex(tabindex) {
    super.__forwardTabIndex(tabindex);
    if (tabindex !== void 0 && this.focusElement) {
      this.focusElement.$.button.tabIndex = tabindex;
      this.focusElement.tabIndex = -1;
    }
  }
  /** @protected */
  ready() {
    super.ready();
    this.addController(this._summaryController);
    this.addController(this._tooltipController);
  }
  /**
   * Override method from `DelegateStateMixin` to set delegate `theme`
   * using attribute instead of property (needed for the Lit version).
   * @protected
   * @override
   */
  _delegateProperty(name, value) {
    if (!this.stateTarget) {
      return;
    }
    if (name === "_theme") {
      this._delegateAttribute("theme", value);
      return;
    }
    super._delegateProperty(name, value);
  }
  /**
   * Override method inherited from `DisabledMixin`
   * to not set `aria-disabled` on the host element.
   *
   * @protected
   * @override
   */
  _setAriaDisabled() {
  }
  /** @private */
  _summaryChanged(summary) {
    this._summaryController.setSummary(summary);
  }
  /** @private */
  __updateAriaAttributes(focusElement, contentElements) {
    if (focusElement && contentElements) {
      const node = contentElements[0];
      if (node) {
        node.setAttribute("role", "region");
        node.setAttribute("aria-labelledby", focusElement.id);
      }
      if (node && node.id) {
        focusElement.setAttribute("aria-controls", node.id);
      } else {
        focusElement.removeAttribute("aria-controls");
      }
    }
  }
};

// node_modules/@vaadin/accordion/src/vaadin-accordion-panel.js
registerStyles("vaadin-accordion-panel", accordionPanel2, { moduleId: "vaadin-accordion-panel-styles" });
var AccordionPanel = class extends AccordionPanelMixin(ThemableMixin(ControllerMixin(PolymerElement))) {
  static get is() {
    return "vaadin-accordion-panel";
  }
  static get template() {
    return html`
      <slot name="summary"></slot>

      <div part="content">
        <slot></slot>
      </div>

      <slot name="tooltip"></slot>
    `;
  }
};
defineCustomElement(AccordionPanel);
/*! Bundled license information:

@vaadin/accordion/src/vaadin-accordion-heading-core-styles.js:
@vaadin/accordion/src/vaadin-accordion-heading.js:
@vaadin/accordion/src/vaadin-accordion-panel-core-styles.js:
@vaadin/accordion/src/vaadin-accordion-panel-mixin.js:
@vaadin/accordion/src/vaadin-accordion-panel.js:
  (**
   * @license
   * Copyright (c) 2019 - 2025 Vaadin Ltd.
   * This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
   *)
*/
//# sourceMappingURL=chunk-FNWPWK3Y.js.map
